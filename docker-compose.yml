services:
  alida-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: alida_novel_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PYTHONUNBUFFERED=1
    env_file:
      - .env
